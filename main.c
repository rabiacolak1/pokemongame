#include <stdio.h>
#include "functions.h"
int main(){
struct Type types[18];
struct Move moves[486];
struct Pokemon pokemons[1015];
struct Player player1;
struct Player player2;
initialize(types,moves,pokemons,&player1,&player2);
    return 0;
}